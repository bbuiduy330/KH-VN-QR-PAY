# Demo runs without environment variables.
VITE_DEMO_MODE=true
# Optional server-only Telegram credentials (NEVER prefix these with VITE_):
TELEGRAM_BOT_TOKEN=
SESSION_SECRET=
# HTTPS Vercel URL for optional local Telegram bot:
WEB_APP_URL=
